<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Users</title>
</head>
<body>
    <a href="/users/create"> create new user </a>
    <table>
        <thead>
            <th>#</th>
            <th>first name</th>
            <th>last name</th>
            <th>email</th>
            <th> show </th>
        </thead>
        <tbody>
            {% for user in users %}
                <tr>
                    <td> {{ user._id }} </td>
                    <td> {{ user.firstName }} </td>
                    <td> {{ user.lastName }} </td>
                    <td> {{ user.email }} </td>
                    <th> <a href="/users/show/{{ user._id }}"> show </a> </th>
                </tr>
            {% endfor %}
        </tbody>
    </table>


</body>
</html>